# MengASR2 Qwen3-ASR pip 约束文件
# 防止 pip install 时意外升级/降级核心依赖
# 部署到 /srv/mengasr/constraints-qwen3.txt
# ⚠️ 与 MiMo 的 constraints.txt 不同（transformers 版本互斥）

torch==2.12.0
torchaudio==2.11.0
transformers==4.57.6
accelerate==1.12.0
qwen-asr==0.0.6
silero-vad==6.2.1
